###############################
# Frontend Environment Config #
###############################

# Porta do frontend (pode sobrescrever via: PORT=4001 npm start)
# Altere abaixo se quiser porta fixa diferente de 3000
PORT=4000

# URL do backend (API)
REACT_APP_BACKEND_URL=http://localhost:3001

# (Opcional) Caso use WebSocket ou outro endpoint separado, adicione:
# REACT_APP_SOCKET_URL=http://localhost:3001

# (Opcional) Base pública para assets/CDN
# REACT_APP_PUBLIC_BASE=/

# OBS: Variáveis precisam começar com REACT_APP_ para serem expostas no código React.

